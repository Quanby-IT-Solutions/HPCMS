/**
 * Re-export of the portal routes constants. Spec PAT-FE-01 lists this path;
 * the canonical implementation lives in `features/portal-shared/lib` so all
 * portal features can share it.
 */
export { PORTAL_ROUTES } from "@/features/portal-shared/lib/portal-routes"
